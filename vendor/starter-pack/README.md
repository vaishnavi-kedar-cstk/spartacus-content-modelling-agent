# Vendored connector content model

These are the **contentstack-spartacus connector's** shipped starter-pack content types,
copied here so the agent is self-contained (no sibling checkout needed).

Source: `contentstack-spartacus-feature/import-export/starter-pack/content_types/`
The connector owns this model. Re-vendor if the connector's content types change.
